CREATE TABLE my_table (
   col1 VARCHAR(50),
   col2 VARCHAR(50),
   col3 VARCHAR(50)
);

INSERT INTO my_table (col1, col2, col3) VALUES
('I', 'Love', 'SQL');

-------------------------------------------------------------

SELECT * FROM my_table;
